#pragma once
#include "Weapon.h"

class SquareBuilder {
	Weapon square;

public:
	// SquareBuilder()
	/*SquareBuilder& translate(float translateX, float translateY);
	SquareBuilder& scale(float scaleX, float ScaleY);
	SquareBuilder& rotate(float rad);*/
};

